{
  "name": "git-viewer",
  "version": "0.1.0",
  "description": "Git repository viewer built with Tauri",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "vitest",
    "test:ui": "vitest --ui"
  },
  "devDependencies": {
    "vite": "^5.0.0",
    "@tauri-apps/api": "^2.0.0",
    "@tauri-apps/plugin-dialog": "^2.0.0",
    "vitest": "^1.0.0",
    "jsdom": "^23.0.0"
  }
}